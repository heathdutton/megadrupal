<?php
/**
 * @file
 * Main module file for the Felix UI module.
 *
 * The User Interface for the Felix module.
 */

/**
 * Implements hook_menu().
 */
function felix_ui_menu() {
  $menu = array();

  $menu['admin/structure/felix'] = array(
    'title' => 'Felix blocks manager',
    'description' => 'Manage felix block sets and regions.',
    'page callback' => 'felix_ui_regions_page',
    'access arguments' => array('administer site configuration'),
    'type' => MENU_NORMAL_ITEM,
    'file' => 'felix_ui.pages.inc',
  );

  $menu['admin/structure/felix/regions'] = array(
    'title' => 'Regions',
    'page callback' => 'felix_ui_regions_page',
    'access arguments' => array('administer site configuration'),
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 1,
  );

  $menu['admin/structure/felix/regions/list'] = array(
    'title' => 'List',
    'page callback' => 'felix_ui_regions_page',
    'access arguments' => array('administer site configuration'),
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 1,
  );

  $menu['admin/structure/felix/regions/add'] = array(
    'title' => 'Add region',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_region_form'),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 2,
  );

  $menu['admin/structure/felix/regions/%/edit'] = array(
    'title' => 'Edit region',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_region_form', 4),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_CALLBACK,
    'file' => 'felix_ui.pages.inc',
  );

  $menu['admin/structure/felix/regions/%/delete'] = array(
    'title' => 'Delete region',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_delete_region_form', 4),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_CALLBACK,
    'file' => 'felix_ui.pages.inc',
  );

  $menu['admin/structure/felix/sets'] = array(
    'title' => 'Block sets',
    'page callback' => 'felix_ui_sets_page',
    'access arguments' => array('administer site configuration'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 2,
  );

  $menu['admin/structure/felix/sets/list'] = array(
    'title' => 'List',
    'page callback' => 'felix_ui_sets_page',
    'access arguments' => array('administer site configuration'),
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 1,
  );

  $menu['admin/structure/felix/sets/add'] = array(
    'title' => 'Add block set',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_set_form'),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'felix_ui.pages.inc',
    'weight' => 2,
  );

  $menu['admin/structure/felix/sets/%/edit'] = array(
    'title' => 'Edit block set',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_set_form', 4),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_CALLBACK,
    'file' => 'felix_ui.pages.inc',
  );

  $menu['admin/structure/felix/sets/%/delete'] = array(
    'title' => 'Delete block set',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('felix_ui_delete_set_form', 4),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_CALLBACK,
    'file' => 'felix_ui.pages.inc',
  );

  return $menu;
}

