<?php

/**
 * Implements hook_theme().
 */
function views_supertable_theme($existing, $type, $theme, $path) {
  return array(
    'views_supertable_view' => array(),
  );
}

/**
 * Implements hook_views_api().
 */
function views_supertable_views_api() {
  return array('api' => '3');
}
