/*
This module doesn't have any hooks. But it might rely on some external one.

By default the module gets URL for content entity from entity_uri function. But you
can easily customize it by setting a property called "smartling_context_url" in hook_entity_load
for example.
*/