<?php

/**
 * Implements hook_menu().
 */
function bookit_menu() {
  $items = array();

  $items['admin/bookit'] = array(
    'title' => 'Booking',
    'description' => 'Administer your booking platform.',
    'page callback' => 'system_admin_menu_block_page',
    'access arguments' => array('access administration pages'),
    'file path' => drupal_get_path('module', 'system'),
    'file' => 'system.admin.inc',
    'weight' => -7,
  );

  $items['admin/bookit/config'] = array(
    'title' => 'Configuration',
    'description' => 'Configure settings for your booking platform.',
    'page callback' => 'system_admin_menu_block_page',
    'access arguments' => array('configure booking'),
    'type' => MENU_NORMAL_ITEM,
    'weight' => 50,
    'file path' => drupal_get_path('module', 'system'),
    'file' => 'system.admin.inc',
  );

  return $items;
}

/**
 * Implements hook_permission().
 */
function bookit_permission() {
  return array(
    'configure booking' =>  array(
      'title' => t('Configure booking settings'),
      'description' => t('Allows users to administer the general settings.'),
      'restrict access' => TRUE,
    ),
  );
}
