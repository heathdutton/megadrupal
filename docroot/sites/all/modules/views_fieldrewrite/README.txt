DESCRIPTION
-----------

This module changes the "Rewrite the output of this field" section in all Views
fields in the UI. To accomplish that it uses the Views Decorator module.

At the moment it provides field rewriting using regular expression patterns and
replacement strings, which may also contain replacement tokens that can use any
existing field output.
