Payment UUID

Overview
--------
The Payment UUID module provides UUID integration for the Payment module. The
module only adds an UUID property to Payment entity.

Install this module if it is needed as dependency of another module or if you
develop code and need Payment entities to have UUIDs.
