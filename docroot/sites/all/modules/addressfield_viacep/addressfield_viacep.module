<?php

/**
 * @file
 * Address Field Via CEP main module file.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_viacep_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
