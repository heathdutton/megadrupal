<?php
/**
 * @file
 * Drupal needs this blank file.
 */

define('QD_SERVICES_VOCABULARY', 'qd_services');

module_load_include('inc', 'qd_core', 'qd_core.debug');

/**
 * Implements hook_menu().
 */
function qd_core_menu() {
  $items = array();

  $items['admin/config/services/qd_core'] = array(
    'title' => 'QD Core',
    'description' => 'Settings for QD Core',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('qd_core_settings_form'),
    'file' => 'qd_core.admin.inc',
    'access arguments' => array('administer'),
  );

  $items['admin/config/services/qd_core/settings'] = array(
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'title' => 'Settings',
    'description' => 'QD Core settings',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('qd_core_settings_form'),
    'file' => 'qd_core.admin.inc',
    'access arguments' => array('administer'),
    'weight' => -1,
  );

  $items['admin/config/services/qd_core/debugsettings'] = array(
    'type' => MENU_LOCAL_TASK,
    'title' => 'Debug Settings',
    'description' => 'Page for developing and debugging QD Core',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('qd_core_debug_settings_form'),
    'file' => 'qd_core.debug.inc',
    'access arguments' => array('administer'),
  );

  $items['admin/config/services/qd_core/debugrun'] = array(
    'type' => MENU_LOCAL_TASK,
    'title' => 'Debug Tool',
    'description' => 'Page for developing and debugging QD Core',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('qd_core_debug_run_form'),
    'file' => 'qd_core.debug.inc',
    'access arguments' => array('administer'),
  );

  return $items;
}
