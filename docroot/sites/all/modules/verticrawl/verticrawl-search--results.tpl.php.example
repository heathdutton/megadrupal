<div class="verticrawl-search-results">
<?php foreach ($urls as $key => $url) : ?>
  <div class="verticrawl-search-item verticrawl-search-item-<?php print ($key % 2 == 0) ? 'even' : 'odd'; ?> verticrawl-search-item-<?php print $key; ?>">
    
    <!-- DISPLAY SCREENSHOT IF AVAILABLE -->
    <!-- Do not remove this code -->
    <?php if(isset($screenshot_url) && isset($url['sshot']) && $url['sshot']==1): ?>
      <div class="verticrawl-search-field verticrawl-search-field--screenshot">
        <a href="<?php print($url['url']) ?>" target="_blank">
          <img rel="nofollow" src="<?php print($screenshot_url . '&rec_id=' . $url['doc_id']); ?>" />
        </a>
      </div>
    <?php endif; ?>
    <!-- DISPLAY SCREENSHOT IF AVAILABLE -->    

    <?php foreach ($url as $field => $value) : ?>
      <?php switch($field) : 
        case 'do-not-remove-this-case' : ?>
          <?php break; ?>

        <!-- PASTE THIS CODE -->
        <!-- In order to adapt the rendering of each variables you need  -->
        <?php case 'some-field-name' : ?>          
            <?php print $value; ?>          
          <?php break; ?>
        <!-- PASTE THIS CODE -->

        <!-- EXAMPLE WITH THE "URL" FIELD -->
        <?php case 'url' : ?>
          <div class="verticrawl-search-field verticrawl-search-field--<?php print(drupal_clean_css_identifier(drupal_strtolower($field))); ?>">
            <a href=<?php print $value; ?> target="_blank"><?php print $value; ?></a>
          </div>
          <?php break; ?>
        <!-- EXAMPLE WITH THE "URL" FIELD -->

        <!-- REMOVE THIS CODE -->
        <!-- You can remove this code if you don't want to render all variables -->
        <?php default: ?>
          <div class="verticrawl-search-field verticrawl-search-field--<?php print(drupal_clean_css_identifier(drupal_strtolower($field))); ?>">
            <?php print $value; ?>
          </div>
          <?php break; ?>
        <!-- REMOVE THIS CODE -->

      <?php endswitch; ?>
  <?php endforeach; ?>
  </div>
<?php endforeach; ?>
</div>