

Installation:
* Enable the module "Directory based access control".
* Enable the block.
* Check the permissions in the corresponding blocks as global access
  permissions of "Directory based organisational layer" will be ignored.
