Install and enable like any other module

Presently, this module will take over all Leaflet maps (I assume). That means if you want some to
have Popups (default Leaflet behavior), write a nice config for this module to allow such selection.

texas-bronius