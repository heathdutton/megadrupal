<?php
/**
 * @file
 * Main file for the Yandex.Webmaster module.
 */

// Load additional functions for Yandex.Webmaster module.
require_once dirname(__FILE__) . '/includes/yandex_webmaster.inc';

/**
 * Implements hook_permission().
 */
function yandex_webmaster_permission() {
  return array(
    'administer yandex_webmaster' => array(
      'title' => t('Administer Yandex.Webmaster'),
      // 'description' => '',
    ),
  );
}

/**
 * Implements hook_menu().
 */
function yandex_webmaster_menu() {
  $items = array();

  $items['admin/config/services/yandex_webmaster'] = array(
    'title' => 'Yandex.Webmaster',
    'description' => 'Configure Yandex.Webmaster settings.',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('yandex_webmaster_admin_settings'),
    'access arguments' => array('administer yandex_webmaster'),
    'type' => MENU_NORMAL_ITEM,
    'file' => 'includes/yandex_webmaster.admin.inc',
  );
  $items['admin/config/services/yandex_webmaster/settings'] = array(
    'title' => 'Settings',
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'weight' => 0,
  );
  $items['admin/config/services/yandex_webmaster/playground'] = array(
    'title' => 'Playground',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('yandex_webmaster_admin_playground'),
    'access arguments' => array('administer yandex_webmaster'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'includes/yandex_webmaster.admin.inc',
    'weight' => 10,
  );

  $items['admin/config/services/yandex_webmaster/add'] = array(
    'title' => 'Add site to Yandex.Webmaster',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('yandex_webmaster_add_site'),
    'access arguments' => array('administer yandex_webmaster'),
    'type' => MENU_CALLBACK,
    'file' => 'includes/yandex_webmaster.admin.inc',
  );
  $items['admin/config/services/yandex_webmaster/remove'] = array(
    'title' => 'Remove site from Yandex.Webmaster',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('yandex_webmaster_remove_site'),
    'access arguments' => array('administer yandex_webmaster'),
    'type' => MENU_CALLBACK,
    'file' => 'includes/yandex_webmaster.admin.inc',
  );

  return $items;
}
