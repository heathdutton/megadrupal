<?php

/**
 * @file
 * Module hook implementations.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function text_splitter_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner === 'ctools' && $plugin_type === 'content_types') {
    return 'plugins/content_types';
  }

  return NULL;
}
