name = Structured data markup (Rich Snippets)
description = Structured data markup - is a standard way to annotate your content so machines can understand it. When your web pages include structured data markup, Google (and other search engines) can use that data to index your content better, present it more prominently in search results, and surface it in new experiences that use Semantic data.
core = 7.x

configure = admin/config/structured-data

dependencies[] = metatag

; Information added by Drupal.org packaging script on 2015-06-16
version = "7.x-1.0-alpha2"
core = "7.x"
project = "structured_data"
datestamp = "1434448383"

