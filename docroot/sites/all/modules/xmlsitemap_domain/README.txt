README
==================

This is a simple module that enables XML sitemap for multiple domains that point to the same Drupal sites. For example,
http://example.com, http://example.us, http://example.net, etc.

To use it, you need to create multiple "XML sitemap" in admin. For each sitemap, sets the correct domain.