<?php

/**
 * @file
 * Chaos Tools HTTP Header Plugins module file
 */

/**
 * Implementation of hook_ctools_plugin_directory().
 */
function ctools_header_plugins_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools'  && !empty($plugin)) {
    return 'plugins/' . $plugin;
  }
}