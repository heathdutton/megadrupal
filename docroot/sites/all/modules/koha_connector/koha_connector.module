<?php

/**
 * @file
 * OPAC Koha connector module.
 */

/**
 * Implements hook_opac_connectors().
 */
function koha_connector_opac_connectors() {
  return array(
    'Koha' => array(
      'id' => 'Koha',
      'name' => t('OPAC connector for koha'),
    ),
  );
  return $connectors;
}

/**
 * Implements hook_koha_connector_plugin_info().
 */
function koha_connector_koha_connector_plugin_info() {
  $plugins = array(
    'concatenation' => array(
      'callback' => 'koha_connector_plugin_concatenation',
      'file' => 'plugins/concatenation.inc',
    ),
    'substring' => array(
      'callback' => 'koha_connector_plugin_substring',
      'file' => 'plugins/substring.inc',
    ),
  );
  return $plugins;
}
