<?php

/**
 * Implements hook_menu().
 */
function dreamhost_dns_menu() {
  $items['admin/config/services/dreamhost/dns'] = array(
    'title' => 'DNS',
    'description' => 'Manage the DNS records you have registered with Dreamhost.',
    'access arguments' => array('administer dreamhost'),
    'page callback' => 'dreamhost_dns_admin_settings',
    'file' => 'dreamhost_dns.admin.inc',
    'type' => MENU_LOCAL_TASK,
  );
  $items['admin/config/services/dreamhost/dns/remove'] = array(
    'title' => 'Remove DNS record',
    'description' => 'Remove a DNS record from Dreamhost.',
    'access arguments' => array('administer dreamhost'),
    'page callback' => 'drupal_get_form',
    'page arguments' => array('dreamhost_dns_remove_form', 6, 7, 8),
    'file' => 'dreamhost_dns.admin.inc',
    'type' => MENU_CALLBACK,
  );
  $items['admin/config/services/dreamhost/dns/add'] = array(
    'title' => 'Add DNS record',
    'description' => 'Add a DNS record from Dreamhost.',
    'access arguments' => array('administer dreamhost'),
    'page callback' => 'drupal_get_form',
    'page arguments' => array('dreamhost_dns_add_form'),
    'file' => 'dreamhost_dns.admin.inc',
    'type' => MENU_LOCAL_ACTION,
  );
  return $items;
}
