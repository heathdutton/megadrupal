<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function panels_frame_test_ctools_plugin_directory($owner, $api) {
  if ($owner == 'panels') {
    return "plugins/$api";
  }
}

function _panels_frame_test_colors() {
  return drupal_map_assoc(array('red', 'green', 'blue'));
}
