<?php

/**
 * @file
 * Creates a basic npr_story node type - useful for pulling from and pushing to NPR API.
 */

/**
 * Implements hook_node_info().
 */
function npr_story_node_info() {
  return array(
    'npr_story' => array(
      'name' => t('NPR Story'),
      'base' => 'npr_story',
      'description' => t('NPR Story.'),
      'locked' => TRUE,
      'title_label' => 'Headline',
    ),
  );
}

/**
 * Implements hook_form().
 */
function npr_story_form($node, $form_state) {
  return node_content_form($node, $form_state);
}

/**
 * Implements hook_preprocess_HOOK() for node.
 */
function npr_story_preprocess_node(&$variables) {
  if ($variables['node']->type == 'npr_story') {
    drupal_add_css(drupal_get_path('module', 'npr_story') . '/css/npr_story.css');
  }
}