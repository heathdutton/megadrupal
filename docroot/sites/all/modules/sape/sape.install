<?php
/**
 * @file
 * Install, update and uninstall functions for the Sape module.
 */

/**
 * Implements hook_uninstall().
 */
function sape_uninstall() {
  $variables = array('sape_enabled', 'sape_file_path', 'sape_charset', 'sape_multi_site', 'sape_host', 'sape_verbose', 'sape_force_show_code', 'sape_blocks_count', 'sape_usercode');
  for ($i = 0; $i < $c; $i++) {
    $variables[] = 'sape_block_n' . $i;
  }

  foreach ($variables as $var_name) {
    variable_del($var_name);
  }
}
