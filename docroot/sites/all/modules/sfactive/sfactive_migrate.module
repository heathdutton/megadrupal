<?php

/**
 * Implements hook_migrate_api().
 */
function sfactive_migrate_migrate_api() {
  return array(
    'api' => 2,
  );
}

/**
 * Implements hook_menu().
 */
function sfactive_migrate_menu() {
  $items['admin/content/migrate/sfactive'] = array(
    'title' => 'Configure sf-active',
    'description' => 'Configure sf-active migrate settings.',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('sfactive_migrate_settings'),
    'access arguments' => array('migration information'),
    'file' => 'sfactive_migrate.admin.inc',
    'type' => MENU_LOCAL_TASK,
  );
  return $items;
}

