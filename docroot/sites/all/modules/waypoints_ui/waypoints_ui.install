<?php
/**
 * @file
 * alpha_module.install
 */

/**
 * Implements hook_enable().
 */
function waypoints_ui_enable() {
  // Need waypoints always added.
  // Don't know when elements will appear and don't care ;).
  variable_set('waypoints_always_add_js', 1);
}
