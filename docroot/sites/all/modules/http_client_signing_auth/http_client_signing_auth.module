<?php

/**
 * Returns a new auth object
 *
 * @param $key
 * @param $secret
 * @return HttpClientSigningAuth
 */
function http_client_signing_auth($key, $secret) {
  return new HttpClientSigningAuth($key, $secret);
}