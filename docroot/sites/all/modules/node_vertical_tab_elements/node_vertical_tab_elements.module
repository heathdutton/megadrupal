<?php

/**
 * @file
 * This module defines the hook to create a ctools content type plugin
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function node_vertical_tab_elements_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
}