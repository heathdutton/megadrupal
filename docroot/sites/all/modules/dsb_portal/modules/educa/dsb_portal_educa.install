<?php

/**
 * @file
 * Module install logic.
 */

/**
 * Implements hook_install().
 */
function dsb_portal_educa_install() {
  // Make sure our module is always called after dsb_portal.
  db_update('system')
    ->fields(array(
      'weight' => 10
    ))
    ->condition('name', 'dsb_portal_educa')
    ->execute();
}

/**
 * Implements hook_uninstall().
 */
function dsb_portal_educa_uninstall() {
  foreach (array(
    'dsb_portal_educa_facets',
  ) as $variable) {
    variable_del($variable);
  }
}
