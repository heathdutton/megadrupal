<?php

/**
 * @file
 *  Defines plugin for Cambodia address.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_kh_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
