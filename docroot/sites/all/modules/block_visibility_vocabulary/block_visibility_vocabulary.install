<?php

/**
 * Implements hook_uninstall().
 */
function block_visibility_vocabulary_uninstall() {
  $vars = array('block_visibility_vocabulary');
  foreach ($vars as $var) {
    variable_del($var);
  }
}
