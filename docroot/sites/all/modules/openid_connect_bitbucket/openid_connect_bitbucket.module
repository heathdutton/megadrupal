<?php
/**
 * @file
 * OpenID Connect Bitbucket module.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function openid_connect_bitbucket_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'openid_connect') {
    return 'plugins/' . $plugin_type;
  }
}
