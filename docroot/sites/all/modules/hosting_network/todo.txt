* See code
* Make network overview fieldsets collapsed by default (variable)
* Create server tasks page
* Create tasks overview tab in network overview
* Allow mass migrations on all servers simultaneously
* Allow mass tasks on sites based on VBO and/or regex on site names
