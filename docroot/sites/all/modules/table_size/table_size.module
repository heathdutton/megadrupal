<?php
/**
 * @file
 * The main file of the Table Size module.
 */

/**
 * Implements hook_menu().
 */
function table_size_menu() {
  return TableSizeHooks::menu();
}

/**
 * Implements hook_permission().
 */
function table_size_permission() {
  return TableSizeHooks::permission();
}

/**
 * Implements hook_theme().
 */
function table_size_theme() {
  return TableSizeHooks::theme();
}

/**
 * Implements hook_mail().
 */
function table_size_mail($key, &$message, $params) {
  TableSizeHooks::mail($key, $message, $params);
}

/**
 * Implements hook_cron().
 */
function table_size_cron() {
  $cron = new TableSizeCron();
  $cron->run();
}