<?php
/**
 * @file
 * Creates a filter for views to filter by a users content permissions.
 * 
 * @author Nicholas Alipaz - http://nicholas.alipaz.net/ (email through contact form)
 */

/**
 * Implements hook_views_api().
 */
function views_node_access_level_views_api() {
  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'views_node_access_level') . '/views',
  );
}
