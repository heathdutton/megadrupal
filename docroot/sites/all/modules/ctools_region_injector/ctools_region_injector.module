<?php
/**
 * @file
 * Code for the CTools region injector module.
 */

/**
 * Tell CTools about what plugins we support.
 */
function ctools_region_injector_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
  return '';
}
