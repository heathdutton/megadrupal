-- NOTES --

If Views Bulk Operations (VBO) is installed prior to installing this
module, bulk deleting of spaces can be done.

-- TAXONOMY --
Do we want to include the taxonomy field OR let user decide to use it?
- Would be good with quick tabs type interface for master view of spaces.
