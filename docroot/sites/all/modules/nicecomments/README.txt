The b2 nice comments module is a very light weight extension for Drupal, enabling 
it will turn comment boxes into simple text areas, which will expand when user 
clicks on it. This way it will give a nice clean look for the page instead of 
displaying all the comment fields and instructions when not needed.

by b2hq.com