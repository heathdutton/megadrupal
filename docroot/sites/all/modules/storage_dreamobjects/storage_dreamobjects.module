<?php

/**
 * Implements hook_storage_services().
 */
function storage_dreamobjects_storage_services() {
  $services = array(
    'dreamobjects' => array(
      'name' => t('DreamHost DreamObjects'),
      'class' => 'StorageDreamObjects',
      'copy' => TRUE,
      'serve_secure' => TRUE,
    ),
  );
  return $services;
}

