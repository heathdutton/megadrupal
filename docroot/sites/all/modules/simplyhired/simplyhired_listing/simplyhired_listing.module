<?php

/**
 * @file job_listings.module
 * TODO: Enter file description here.
 */

/**
 * Implements hook_help().
 */
function simplyhired_listing_help($path, $arg) {
  switch ($path) {
    // Main module help for the block module
    case 'admin/help#job_listing':
      return '<p>' . t('Provides a Job Listing content type to display jobs from SimplyHired with pre-defined search criteria.') . '</p>';
	}
}

/**
 * Implements hook_menu().
 */
function simplyhired_listing_menu() {
  $items = array();

  return $items;
}

require_once 'simplyhired_listing.node.inc';
require_once 'simplyhired_listing.theme.inc';
