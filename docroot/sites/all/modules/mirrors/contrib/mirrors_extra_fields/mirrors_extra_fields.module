<?php

/**
 * @file
 * Mirror Extra Fields.
 */

/**
 * Implements ctools_plugin_directory().
 */
function mirrors_extra_fields_ctools_plugin_directory($module, $plugin) {
  if ($module == 'mirrors') {
    return "plugins/$plugin";
  }
}
