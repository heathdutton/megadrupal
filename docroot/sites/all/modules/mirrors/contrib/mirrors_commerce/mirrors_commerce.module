<?php

/**
 * @file
 * Mirrors Commerce.
 */

/**
 * Implements ctools_plugin_directory().
 */
function mirrors_commerce_ctools_plugin_directory($module, $plugin) {
  if ($module == 'mirrors') {
    return "plugins/$plugin";
  }
}
