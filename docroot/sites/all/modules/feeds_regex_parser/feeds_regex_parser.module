<?php
// $Id: feeds_regex_parser.module,v 1.2 2010/09/29 19:13:38 twistor Exp $

/**
 * Implementation of hook_feeds_plugins().
 */
function feeds_regex_parser_feeds_plugins() {
  $info = array();

  $info['FeedsREGEXParser'] = array(
    'name'        => t('REGEX Parser'),
    'description' => t('Parse anything using regular expressions.'),
    'handler'     => array(
      'parent' => 'FeedsParser',
      'class'  => 'FeedsREGEXParser',
      'file'   => 'FeedsREGEXParser.inc',
      'path'   => drupal_get_path('module', 'feeds_regex_parser'),
    ),
  );
  return $info;
}

/**
 * Implementation of hook_enable().
 *
 * Clear Feed's plugin cache so that this plugin shows up.
 */
function feeds_regex_parser_enable() {
  cache_clear_all('plugins:feeds:plugins', 'cache');
}
