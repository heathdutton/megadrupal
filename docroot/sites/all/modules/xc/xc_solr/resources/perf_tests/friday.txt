http://stackoverflow.com/questions/650238/how-to-show-the-last-queries-executed-on-mysql
(the opposite of ON/TABLE is OFF/FILE)

mysql -u root --password=root -D mysql -e "truncate general_log"
mysql -u root --password=root -D mysql -e "select count(*) from general_log"
mysql -u root --password=root -D mysql -e "select * from general_log order by event_time desc \G" > mysql.out

XC_QUERY=zxc; curl -w "\ntime_total: %{time_total}\ntime_namelookup: %{time_namelookup}\ntime_connect: %{time_connect}\ntime_pretransfer: %{time_pretransfer}\ntime_starttransfer: %{time_starttransfer}\n" -gs "http://128.174.70.240/xc/?q=xc/search/${XC_QUERY}&os[keys]=${XC_QUERY}&online_only=0&exclude_microform=0" | tail -n 5

zxc = 400ish
charles = 600ish

    mysql
        there are 115 mysql queries (selects, inserts, and updates) during a search with no results
        there are 810 mysql queries during a search with results
    clicking search sends a POST which is redirected to a GET
        that doesn't make sense and is adding 200 ms

