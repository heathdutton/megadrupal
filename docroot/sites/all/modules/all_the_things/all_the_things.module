<?php

/**
 * Implements hook_form_FORM_ID_alter().
 */
function all_the_things_form_system_performance_settings_alter(&$form, &$form_state, $form_id) {
  $form['actions']['submit']['#value'] = t('MAKE ALL THE THINGS FASTER!');
  $form['clear_cache']['clear']['#value'] = t('CLEAR ALL THE THINGS!');
}

/**
 * Implements hook_form_FORM_ID_alter().
 */
function all_the_things_form_node_type_form_alter(&$form, &$form_state, $form_id) {
  $form['actions']['submit']['#value'] = t('CREATE ALL THE THINGS!');
  $form['actions']['save_continue']['#value'] = t('CREATE ALL THE THINGS AND ADD ALL THE THINGS TO THEM!');
}

/**
 * Implements hook_form_FORM_ID_alter().
 */
function all_the_things_form_user_admin_permissions_alter(&$form, &$form_state, $form_id) {
  $form['actions']['submit']['#value'] = t('ALLOW ALL THE THINGS!');
}

/**
 * Implements hook_form_FORM_ID_alter().
 */
function all_the_things_form_user_admin_roles_alter(&$form, &$form_state, $form_id) {
  $form['actions']['submit']['#value'] = t('ORDER ALL THE THINGS!');
  $form['add']['#value'] = t('ADD A NEW ROLE TO ALL THE THINGS!');
}

/**
 * Implements hook_form_FORM_ID_alter().
 */
function all_the_things_form_node_admin_content_alter(&$form, &$form_state, $form_id) {
  $form['filter']['filters']['status']['actions']['submit']['#value'] = t('FILTER ALL THE THINGS!');
  $form['admin']['options']['submit']['#value'] = t('UPDATE ALL THE THINGS!');
}

/**
 * Implements hook_form_alter().
 */
function all_the_things_form_alter(&$form, &$form_state, $form_id) {
  if ($form['#theme'] == 'confirm_form') {
    $form['actions']['submit']['#value'] = t('SAVE ALL THE THINGS!');
    $form['actions']['cancel']['#title'] = t('CANCEL ALL THE THINGS!');
  }
}

