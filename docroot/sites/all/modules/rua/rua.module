<?php

/**
 * @file
 * Simple module that includes a JS file, all magic happens there.
 */

/**
 * Implements hook_preprocess_page().
 */
function rua_preprocess_page(&$variables) {
  drupal_add_js(drupal_get_path('module', 'rua') . '/js/jquery.rua.js');
}
