<?php

// @todo: this module is not functional.

/**
 * @file
 *   Export/import translatable cck fields.
 */

/**
 * Implements hook_menu().
 */
function xliff_fields_menu() {
  $items['admin/config/regional/xliff/fields'] = array(
    'title' => 'Fields',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('xliff_fields_settings'),
    'access callback' => 'user_access',
    'access arguments' => array('administer xliff'),
    'type' => MENU_LOCAL_TASK,
    'weight' => 2,
    'file' => 'includes/admin.inc',
  );
  return $items;
}
