<?php

/**
 * @file
 *   XLIFF bulk import and export functionality.
 */

/**
 * Implements hook_menu().
 */
function xliff_bulk_menu() {
  $items['admin/config/regional/xliff/bulk-export'] = array(
    'title' => 'XLIFF bulk export',
    'description' => 'Export content into XLIFF format.',
    'page callback' => 'xliff_bulk_export_page',
    'access callback' => 'user_access',
    'access arguments' => array('xliff bulk export'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'includes/export.admin.inc',
  );
  /* $items['admin/config/regional/xliff/bulk-import'] = array(
    'title' => 'XLIFF bulk import',
    'description' => 'Import content form XLIFF format',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('xliff_bulk_import_form'),
    'access callback' => 'user_access',
    'access arguments' => array('xliff bulk import'),
    'type' => MENU_LOCAL_TASK,
    'file' => 'includes/import.admin.inc',
  ); */

  return $items;
}

/**
 * Implements hook_permission().
 */
function xliff_bulk_permission() {
  return array(
    'xliff bulk export' => array(
      'title' => t('XLIFF bulk export'),
    ),
    'xliff bulk import' => array(
      'title' => t('XLIFF bulk import'),
    ),
  );
}
