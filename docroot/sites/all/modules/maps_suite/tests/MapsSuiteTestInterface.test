<?php

/**
 * @file
 * Interface for unit and functional tests on MaPS Suite modules.
 */

/**
 * Interface for test classes related to MaPS Suite module and submodules.
 *
 * Note: please do not use t() function for rendering test messages. For
 * variable replacement, you can use format_string(). This could permit to
 * easily go back to the t() function if it becomes necessary.
 *
 * @example format_string('My message with %variable.',
 * array('%variable' => $my_variable));
 */
interface MapsSuiteTestInterface {

  /**
   * Defines the name of the unit and functional tests group.
   */
  const TEST_GROUP = 'MaPS Suite';

}
