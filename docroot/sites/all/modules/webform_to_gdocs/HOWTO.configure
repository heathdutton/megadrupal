1. Goto console.developers.google.com create a new project fill out the following
   a. Give it a name (anything will do)
   b. Enable the Drive API (this is all you need)
   c. Create a new Client ID
      i. It's a web application
      ii. Javascript origins are your domain name
      iii. Authorized redirect is <you domain from ii.>/webform_to_gdocs/oath2callback with a newline at the end
2. In the admin page feed the client details from the credentials screen and set the rest of the data.
3. Click the authorize button, and you are done!
