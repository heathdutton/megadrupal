{#

  Render me:

    $output = at_theming_render_template('@atest_theming/templates/twig/block.html.twig');

#}

Block content only:

{{ 'system:powered-by' | drupalBlock(1) }}

Block with full template

{{ 'boxes:demo-drupalbox' | drupalBlock }}
