<?php
/**
 * @file
 * Module 2gis.
 */

require_once __DIR__ . '/dgis.form.inc';
require_once __DIR__ . '/dgis.functions.inc';

define('DGIS_PAGE_SIZE_MIN', 5);
define('DGIS_PAGE_SIZE_MAX', 50);
define('DGIS_DEFAULT_CITY', 'Новосибирск');

/**
 * Implements hook_menu().
 */
function dgis_menu() {
  $items['admin/config/services/2gis'] = array(
    'title' => '2GIS',
    'description' => 'Settings for the integration with 2GIS services.',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('dgis_settings_form'),
    'access arguments' => array('administer site configuration'),
    'type' => MENU_NORMAL_ITEM,
  );

  return $items;
}
