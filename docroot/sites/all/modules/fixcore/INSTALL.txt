
INSTALL.txt for Fix Core 6.x-1.x


Enable the module as usual. All fixes are turned off by default.
The entry on the Modules page has a link to the module's settings page,
where you can read about and turn on or off each fix independently.

