<?php

/**
 * @file
 * Schema Kill module file.
 */

/**
 * Implements hook_menu().
 */
function schema_kill_menu() {

  $items['admin/structure/schema/kill'] = array(
    'title' => 'Kill',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('schema_kill_kill_form'),
    'access arguments' => array('administer schema'),
    'file' => 'schema_kill.admin.inc',
  );

  return $items;
}
