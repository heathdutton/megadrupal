<?php

/**
 * @todo Tests to write:
 *  1. Given the freqency since last build has passed
 *     When a build is triggered.
 *     Then the build should run.
 *
 *  2. Given the freqency since last build has NOT passed
 *     When a build is triggered.
 *     Then the build should NOT run.
 *
 *  - Test creating CI Server.
 *  - Test creating build schedule.
 *  - Test build trigger logic.
 *
 *  How do we mimic a Jenkins server?  Is there a free cloud version?  Or do we need to mimic the responses?
 */