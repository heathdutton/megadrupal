<?php

/**
 * Implements hook_views_api().
 */
function views_entity_rules_views_api() {
  return array(
    'api' => '3',
  );
}

/**
 * Implements hook_entity_rules_event_info().
 */
function views_entity_rules_entity_rules_event_info() {
  return array(
    'views_field_visibility' => array(
      'label' => t('Views field visibility'),
      'description' => t('Views field is about to be shown.'),
      'needs_form' => FALSE,
      'component_types' => array('rule', 'rule set'),
      'vars' => array(
        array(
          'type' => 'boolean',
          'label' => 'Show field',
          'name' => 'show_field',
          'usage' => '11',
          'weight' => 1,
          'description' => t('If set to FALSE, the field will be hidden.'),
          'entity_rules_settings' => array('default_value' => TRUE),
        ),
      ),
    ),
  );
}
