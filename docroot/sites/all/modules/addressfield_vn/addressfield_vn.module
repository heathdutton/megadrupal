<?php

/**
 * @file
 * Defines plugin for Viet Nam address.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_vn_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
