TODO (2011-10-15):
==================

 * Add the jquery stub (can we stub jquery ui too?)

 * Admin page:
   o Enable/disable the optimizr
   o Configure path to optimizer
   o Clear cached optimized files
 
 * Cached files should be cleared by the normal mechanism (especially Drush)

 * Documentation
   o How to install and configure
   o Be careful with filenames and .js!
   o hooks
   o Public function API documentation
   o Include an example module (requirejs_example)

