Support Dashboard README
------------------------

This module is quite simple currently. It, basically, mirrors the existing support module client pages in an all-client form at URL.tld/support_dashboard.