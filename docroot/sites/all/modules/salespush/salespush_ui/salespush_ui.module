<?php

/**
 * @file
 * User Interface routines for SalesPush module.
 */

/**
 * Implements hook_menu().
 */
function salespush_ui_menu() {
  $items['admin/config/services/salespush'] = array(
    'title' => 'PUSH settings',
    'description' => 'Salesforce PUSH forms settings',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_admin'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.admin.inc',
  );
  $items['admin/config/services/salespush/settings'] = array(
    'title' => 'SalesForce API settings',
    'type' => MENU_DEFAULT_LOCAL_TASK,
    'weight' => -10
  );
  $items['admin/config/services/salespush/environment'] = array(
    'title' => 'PUSH environment settings',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_environment'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.admin.inc',
  );

  $items['admin/config/content/salespush'] = array(
    'title' => 'Salesforce PUSH',
    'description' => 'Salesforce PUSH forms',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_content_overview'),
    'access arguments' => array('administer site configuration'),
    'weight' => -10
  );

  $items['admin/config/content/salespush/sandbox'] = array(
    'title' => 'Sandbox: Users (1)',
    'type' => MENU_LOCAL_TASK,
    'description' => 'Salesforce PUSH forms',
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_users_sandbox'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.users.inc',
  );

  $items['admin/config/content/salespush/production'] = array(
    'title' => 'Production: Users (1)',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_users_production'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.users.inc',
  );

  $items['admin/config/content/salespush/sandbox/userids'] = array(
    'title' => 'Users IDs sync (2)',
    'type' => MENU_LOCAL_TASK,
    'weight' => -10,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_users_sandbox_id_sync'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.users.inc',
  );

  $items['admin/config/content/salespush/production/userids'] = array(
    'title' => 'Users IDs sync (2)',
    'type' => MENU_LOCAL_TASK,
    'weight' => -10,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_users_prod_id_sync'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.users.inc',
  );

  $items['admin/config/content/salespush/sandbox/campaigns'] = array(
    'title' => 'Campaigns push (3)',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_campaigns_sandbox'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.campaigns.inc',
  );

  $items['admin/config/content/salespush/production/campaigns'] = array(
    'title' => 'Campaigns push (3)',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_campaigns_production'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.campaigns.inc',
  );

  $items['admin/config/content/salespush/sandbox/submissions'] = array(
    'title' => 'Webform submissions push (4)',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_submissions_sandbox'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.submissions.inc',
  );
  $items['admin/config/content/salespush/production/submissions'] = array(
    'title' => 'Webform submissions push (4)',
    'type' => MENU_LOCAL_TASK,
    'page callback' => 'drupal_get_form',
    'page arguments' => array('salespush_ui_submissions_production'),
    'access arguments' => array('administer site configuration'),
    'file' => 'salespush_ui.submissions.inc',
  );

  return $items;
}

/**
 * Wrapper for default menu.
 */
function salespush_ui_content_overview($form, &$form_state) {
  $form['salespush_overview'] = array(
    '#markup' => t('Select environment to process.'),
  );

  return $form;
}
