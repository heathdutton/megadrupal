name = Simple LDAP Active Group
description = Adds a user to the defined LDAP group when set to "Active" and removes the user from the group when set to "Blocked". This module is best used when a search filter is set in Simple Ldap User to enforce group membership. "memberOf=cn=active,ou=groups,o=example" for example.
package = LDAP Helpers
core = 7.x
configure = admin/config/people/simple_ldap/active_group
dependencies[] = simple_ldap_user
dependencies[] = simple_ldap_role

; Information added by drupal.org packaging script on 2013-05-14
version = "7.x-1.4"
core = "7.x"
project = "simple_ldap"
datestamp = "1368556212"

