<?php

/**
 * @file
 * take_control.module file, which produces the primary functionality.
 * Inspired by myself on being fed up to take control of Drupal files directory somehow.
 *
 */

/**
 * Implementation of hook_perm().
 */
function take_control_permission() {
  return array(
      'administer take control' => array ('title' => 'Administer take control settings'),
      'access take control' => array ('title' => 'Access take control module'),
  );
}
