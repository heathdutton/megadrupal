<?php

/**
 * @file
 * Module file for Address Field Sub Promise.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function addressfield_sub_premise_ctools_plugin_directory($module, $plugin) {
  if ($module == 'addressfield') {
    return 'plugins/' . $plugin;
  }
}
