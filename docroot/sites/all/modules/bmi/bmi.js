  Drupal.Ajax.plugins.bmi = function(hook, args) {
      if (hook === 'complete' && args.status == true) {
//		document.getElementById("bmi_output").html="testing";		  
//		  alert("test");
//		$('bmi_output').html(<div style="display:block">testing</div>);
//		$('bmi_output').attr(
/*        var content_type = $('.oap_title').attr('content_type');
        var update_div = ".content_"+content_type;
        reload_oap_content_types(content_type,update_div);
*/		
      }
    }
