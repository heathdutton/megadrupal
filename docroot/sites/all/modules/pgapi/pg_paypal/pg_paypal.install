<?php

/**
 * @file
 * Install, update and uninstall functions for the pg_paypal module.
 */

/**
 * Implements hook_uninstall().
 */
function pg_paypal_uninstall() {
  db_delete('variable')
    ->condition('name', 'pg_paypal_%', 'LIKE')
    ->execute();
  cache_clear_all('variables', 'cache_bootstrap');
}
