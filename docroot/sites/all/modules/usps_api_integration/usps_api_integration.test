<?php 

/**
 * @file
 * Tests for usps_api_integration.module.
 */

class UspsApiIntegrationTestCase extends DrupalWebTestCase {
  protected $instance;
  protected $webUser;

  /**
   * Set simple test data.
   */
  public static function getInfo() {
    return array(
      'name'  => 'USPS API Integration Test',
      'description'  => 'Test the USPS API module is set up correctly and returning data.',
      'group' => 'USPS Tests',
    );
  }

  /**
   * Prepare for testing.
   */
  public function setUp() {
    parent::setUp(array('usps_api_integration'));
    $this->webUser = $this->drupalCreateUser(array('administer usps'));
    $this->drupalLogin($this->webUser);
  }

  /**
   * Test the connection to USPS.
   */
  public function testConnection() {

  }

  /**
   * Test that the USPS key is correct.
   */
  public function testKey() {

  }
}
