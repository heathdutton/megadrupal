<?php
// $id$

/**
 * Implements hook_ctools_plugin_directory().
 */
function uc_panels_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
}
