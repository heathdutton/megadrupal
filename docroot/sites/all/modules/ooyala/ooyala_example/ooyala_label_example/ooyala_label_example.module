<?php
/**
 * @file
 * Drupal needs this blank file.
 */

/**
 * Implements hook_enable().
 */
function ooyala_label_example_enable() {
  drupal_set_message(t('Please verify your <a href="@sync-url">Taxonomy Vocabulary Syncing settings</a> to ensure that the Tags field is being synced with the right set of Labels in the Ooyala backlot.', array('@sync-url' => url('admin/config/media/ooyala/settings'))));
}

