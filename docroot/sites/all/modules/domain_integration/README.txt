domain
- rules integration
- views integration
- search API integration

Methode:
Entity API info aanpassing voor nodes en users om de domains te tonen.
Bij beide wordt een RAW versie getoond (domain id's), en een normale versie (domain sitenames)

Rules:
maakt het mogelijk om in rules van nodes en users het domain op te vragen voor bv data comparisons

Views:
Maakt het mogelijk om domain te tonen als field, te gebruiken als filter of contextual filter

Search API:
Domain is te indexeren en kan gebruikt worden als een facet
