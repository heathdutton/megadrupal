// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
TODO
// /////////////////////////////////////////////////////////////////////////////////////////////////////////////


add theme dependencies with alerts
rework the way media queries work so they work in IE
create css generation function so that is generates css and stores it instead of including stylesheets



css3-mediaqueries shiv for IE
Update mobile only theme
Add in micro formats, microformats meta tags for profiles
Create plinko extras module for settings by path
Check and tweak css defaults for headings, copy, tables, form items. 
Theme default search results 
add in preprocess hooks to get HTML5 form elements by default. (integration with HTML5 tools)
remove manual styles for now
research Drupal RDFA and html5 and update accordingly
Administration pages css testing and tweaking
yslow testing
