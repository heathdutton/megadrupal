Drupal.behaviors.MYTHEME = {
  attach: function(context, settings) {
    (function ($) {

    
    })(jQuery);
  } // attach
};

// OBJECT DEFINITIONS
// --------------------------------------------------------------------------------------------


var MYTHEME = MYTHEME || {};