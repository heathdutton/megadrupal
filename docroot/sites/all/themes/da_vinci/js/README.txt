js:
===
Contains the libraries and also the js plugins.

	da-vinci.js:
	==========
  Da Vinci JS Theme, menu mobile and styles for messages and krumo messages.

	Plugins (Libraries Folder):
	========

		css_browser.js:
		===============
    This plugin can identify the browser that is being used for view the
    website,
    and gives us the necessary classes in the body for can give specific
    styles.
    If we need specific class for a concrete version of any browser,
    this is our file.

		html5.js:
		=========
    For those browsers that don't support HTML5, this file gives them the
    capacity to can identify and read them.

  chosen.js:
  ==========
  Library for the chosen jQuery.

	custom.js:
	==========
	When we'll need modify our javascript, we should insert the code in
  this file.

  masonry-view.js:
  ================
  Necessary functions for masonry remove method. If we want to implement
  the infinite scroll,
  we will need to install the "views_infinite_scroll" and "autopager"
  module.

	modales.js
	===========
  Necessary functions for open the modal windows. If we need a new modal,
  this is our file.
  The content of the 'styleguide' module was converted in a modal.
  See the interior of the file to read more information about 'styleguide'.


===========
  ESPAÑOL
===========

js:
===
Contiene tanto las librerias como los plugins js necesarios.

	da-vinci.js:
	==========
  Da Vinci JS Theme, menu movil y estilos para los mensajes y mensajes de krumo.

	Plugins:
	========

		css_browser.js:
		===============
		Este plugin identifica el navegador que está siendo usado para visualizar
    el portal y aporta las clases necesarias en el body para poder dar estilos
    específicos. 
    
    Si necesitamos sacar clases específicas para alguna versión en concreto de
    cualquier navegador, este es nuestro archivo.

		html5.js:
		=========
    Para aquellos navegadores que no soportan HTML5, este fichero les da la
    capacidad de poder identificarlas y leerlas.

  chosen.js:
  ==========
	Librería dedicada al chosen de jQuery.

	custom.js:
	==========
	Cuando necesitemos modificar nuestro javascript,
  insertaremos el código en este fichero.

  masonry-view.js:
  ================
  Funciones para el metodo remove de masonry, si queremos implementar el scroll
  infinito necesitamos instalar el modulo "views_infinite_scroll" 
  y el "autopager".

	modales.js
	===========
	Funciones necesarias para lanzar las ventanas modales.
  Si necesitamos una nueva ventana modal, este es el archivo.
  Se ha convertido el contenido del modulo "styleguide" en una ventana modal.
  Ver el interior del archivo para leer más sobre "styleguide".
.
