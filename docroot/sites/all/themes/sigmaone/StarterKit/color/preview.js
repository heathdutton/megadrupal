(function ($) {
  Drupal.color = {
    callback: function(context, settings, form, farb, height, width) {
      // Put my custom jQuery script to change the preview.html
      // color when user selected the color change in the color
      // module GUI.
    }
  };
})(jQuery);
